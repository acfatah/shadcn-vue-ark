# Keep in mind that all VITE_ prefixed variables are exposed to the client-side,
# which means they will be included in the final build and can be seen by anyone
# who inspects your application.

VITE_PAGE_TITLE=
VITE_API_URL=http://localhost:3000

# shadcn registry address (<owner>/<repo>) for `bun ui`.
# Optional: REGISTRY_REF (branch/tag/sha, default main),
# REGISTRY_CATALOG_URL to point at a custom catalog host.
REGISTRY=acfatah/shadcn-vue-ark
