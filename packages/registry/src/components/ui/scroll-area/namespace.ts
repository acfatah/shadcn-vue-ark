import ScrollAreaRoot from './ScrollArea.vue'
import ScrollBar from './ScrollBar.vue'

export const ScrollArea = {
  /** ScrollArea root provider */
  Root: ScrollAreaRoot,

  /** ScrollArea scrollbar */
  Scrollbar: ScrollBar,
}
